{{plugin-outlet name="shortcut-nav" tagName=""}}


{{plugin-outlet name="above-site-header" tagName=""}}
{{site-header canSignUp=canSignUp
              showCreateAccount=(route-action "showCreateAccount")
              showLogin=(route-action "showLogin")
              showKeyboard=(route-action "showKeyboardShortcutsHelp")
              toggleMobileView=(route-action "toggleMobileView")
              toggleAnonymous=(route-action "toggleAnonymous")
              logout=(route-action "logout")
             }}

{{plugin-outlet class="" name="below-site-header" args=(hash currentPath=router._router.currentPath) tagName=""}}

<div id="main-outlet" class="wrap">
  {{plugin-outlet name="above-main-container"}}
  <div class="container">
    {{#if showTop}}
      {{custom-html name="top"}}
    {{/if}}
    {{notification-consent-banner}}
    {{pwa-install-banner}}
    {{global-notice}}
    {{create-topics-notice}}
    {{plugin-outlet name="top-notices" args=(hash currentPath=router._router.currentPath)}}
  </div>
  {{outlet}}
  {{outlet "user-card"}}
</div>

{{plugin-outlet name="above-footer" args=(hash showFooter=showFooter) tagName=""}}
{{#if showFooter}}
  {{custom-html name="footer" triggerAppEvent="true" tagName=""}}
{{/if}}
{{plugin-outlet name="below-footer" args=(hash showFooter=showFooter) tagName=""}}

{{outlet "modal"}}
{{topic-entrance}}
{{outlet "composer"}}

{{#if showFooterNav}}
  {{footer-nav}}
{{/if}}
